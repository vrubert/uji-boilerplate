Ext.define('{{applicationName}}.store.{{#pluralize}}{{modelName}}{{/pluralize}}',
{
    extend : 'Ext.data.Store',
    alias : 'store.{{instanceName}}',
    model : '{{applicationName}}.model.{{#pluralize}}{{modelName}}{{/pluralize}}',

    proxy :
    {
        type : 'rest',
        url : '/{{applicationName}}/rest/{{#pluralize}}{{instanceName}}{{/pluralize}}',
        reader :
        {
            type : 'json',
            rootProperty : 'data'
        },
        writer :
        {
            type : 'json',
            writeAllFields : true
        }
    }
});
